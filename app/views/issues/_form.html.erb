<%= form_with(model: issue, local: true) do |form| %>
  <% if issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(issue.errors.count, "error") %> prohibited this issue from being saved:</h2>

      <ul>
      <% issue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :headline %>
    <%= form.text_field :headline, id: :issue_headline %>
  </div>

  <div class="field">
    <%= form.label :released_at %>
    <%= form.date_select :released_at, id: :issue_released_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
